class ExcelManager:
    def __init__(self, file_path):
        self.file_path = file_path

    def load_data(self):
        # Logique pour lire openpyxl plus tard
        print(f"Chargement des données depuis {self.file_path}")
        return []